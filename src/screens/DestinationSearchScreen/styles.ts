import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  container: {
    margin: 20,
    height: '100%',
  },
  textInput: {
    fontSize: 20,
    marginBottom: 20,
  },
});

export default styles;
