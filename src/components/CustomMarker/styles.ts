import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  markerBackground: {
    padding: 5,
    borderRadius: 14,
    borderColor: 'grey',
    borderWidth: 2,
  },
  markerLabel: {
    fontWeight: 'bold',
  },
});

export default styles;
